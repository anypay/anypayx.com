version: 2.1

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master

jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: cd nextjs_TS && npm install

      - run:
          name: Build Static App
          command: cd nextjs_TS && npm run export

      - run:
          name: Run Tests
          command:  echo "no tests"

  deploy:
    machine:
      image: ubuntu-18
    steps:
      - checkout
      - add_ssh_keys
      - run:
          name: Update Live Container
          command: cde nextjs_TS && ./deploy.sh

